const array = [1, 3, 4, 5, 6, 7, ]
const numero = 7
const numerofiltrado = array.filter(numeros => numeros !== numero)
console.log(numerofiltrado)

const verdade = array.some(numeros => numeros === 29)
console.log(verdade)